## 1. App 日志规范化（`src/App.tsx`）

- [x] 1.1 增加统一日志前缀与轻量日志辅助函数（event + payload）
- [x] 1.2 替换/规范 `App` 中现存的 `console.log`：统一事件名与 payload，避免大对象直出

## 2. Verify

- [x] 2.1 运行 `pnpm test:run`
- [x] 2.2 运行 `pnpm build`
